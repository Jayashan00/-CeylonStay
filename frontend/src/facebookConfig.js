// Paste your Facebook App ID here (from developers.facebook.com → your app → Settings → Basic).
// This is safe to expose in frontend code — unlike the App Secret, which
// only ever lives on the backend.
export const FACEBOOK_APP_ID = '4031591476976211'